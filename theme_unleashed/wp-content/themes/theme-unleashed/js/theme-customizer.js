/*
JavaScript интерактива в настройках темы
*/

( function ( jQuery ) {

	// wp.customize( 'YOUR_SETTING_ID', function( value ) {
	// 	value.bind( function( newval ) {
	// 		//Do stuff (newval variable contains your "new" setting data)
	// 	} );
	// } );

} ) ( jQuery );